name: state_management_practice
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2  
  firebase_auth: ^4.17.7
  hive: ^2.2.3
  path_provider: ^2.1.0
  hive_flutter: ^1.1.0
  auto_size_text: ^3.0.0
  get: ^4.7.2
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets: 
    - assets/icons/
  uses-material-design: true
